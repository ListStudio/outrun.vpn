name: outrun_vpn
version: 1.0.0
description: A comprehensive Flutter VPN app

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^5.0.0
  # Other dependencies can be added here

flutter:
  uses-material-design: true
