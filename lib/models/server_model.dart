class Server {
  String name;
  String country;
  String protocol;
  int ping;
  String subscription;

  Server({required this.name, required this.country, required this.protocol, required this.ping, required this.subscription});
}